{
  // ==========================================================================
  // Polyagent DevContainer — Debug Configurations
  // ==========================================================================
  // Add debug configurations to match your project.
  // Python: debugpy, Node.js: node, Go: dlv
  // ==========================================================================
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Node.js: current file",
      "type": "node",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
